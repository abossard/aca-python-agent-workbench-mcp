[project]
name = "aca-python-agent-workbench-tests"
version = "0.1.0"
description = "Tests for Python Aspire Agent Workbench"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "httpx>=0.27.0",
]
